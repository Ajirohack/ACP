{
  "name": "control-plane-cli",
  "version": "1.0.0",
  "description": "A CLI for interacting with the Android Virtual OS Control Plane.",
  "main": "index.js",
  "bin": {
    "cp-cli": "./index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "commander": "^11.1.0",
    "dotenv": "^16.3.1"
  }
}
